# AGENTS.md

本工作空间是**前端知识体系学习库**（非代码项目）。内容为中文 Markdown 笔记，语言无关（v1 用伪代码，v2 绑定具体技术栈）。所有笔记计划提交至 GitHub。

## 仓库定位
- 不是代码仓库，是**知识体系文档库**。无构建、无测试、无依赖。
- 风格：**原理优先**（讲清"为什么这样设计"，不只记结论），深度优先于篇幅。
- 受众：供人类系统阅读学习，建立完整前端认知框架。

## 目录结构（5 大模块）
```
00-foundation/     地基与心智（世界观、两条主轴、权衡、复杂度、需求生命周期）
01-pillars/        核心支柱（13 个子目录，01-1 到 01-13）
02-advanced/       前端进阶（SSR/大型应用/性能极致/复杂交互）
03-engineering/    工程实践（需求流程/构建发布/测试/CI-CD/前后端协作）
09-prerequisites/  支撑基础（浏览器/网络/JS，按需查阅）
```
每个模块目录有一个 `README.md`（目录索引，含可点击链接）+ 若干原子笔记 `.md`。

## 笔记结构约定（★ 必须严格遵守）
每篇原子笔记必须包含以下五段，顺序固定：
1. `## 是什么` — 一句话定义 + 精确边界
2. `## 为什么（原理推导）` — 来由、权衡、原理直觉，不直接抛结论
3. `## 怎么工作 / 怎么用` — 机制细节，代码用**伪代码**（v1 不绑定语言）
4. `## 常见坑` — 实践陷阱，用 `❌反例 → ✅正例` 对比
5. `## 关联（双向打通）` — 必须填实，三个子项缺一不可：
   - `- 依赖 ↓：`理解前需先懂的概念（带相对路径链接，往地基/底层走）
   - `- 属于 ↑：`在知识树中的位置（往主干/支柱走）
   - `- 相关 →：`兄弟或交叉概念（带链接）

开头用 `>` 引用块写一句话定位（这篇在整个体系里的位置/价值）。

## 链接规范
- 全部用**相对路径**（如 `./two-axes.md`、`../01-4-rendering/declarative-rendering.md`）。
- 不得有死链。新增笔记后必须更新所在模块的 `README.md`，把大纲项变成指向该笔记的链接。
- 跨模块概念用交叉引用勾连（如"性能"同时出现在 01-9 和 02-3，互相链接，不重复写）。

## 两条主轴（体系核心心智锚点）
所有知识点应能归位到：
- **视图轴**：怎么描述 / 怎么组织 / 怎么渲染 / 怎么不卡（主覆盖 01-1、01-2、01-3、01-4）
- **状态轴**：从哪来 / 怎么存 / 怎么变 / 怎么同步到视图（主覆盖 01-5、01-6、01-7）
- **两轴交汇**：01-8 交互与表单（操作→状态→视图 的闭环）
- **横切**：01-9 性能体验、01-10 可访问性多端、01-11 安全、01-12 架构工程化、01-13 可观测质量

## 粒度约定（编辑时注意）
- **00 地基层**：原子粒度，一个概念一篇，原理写透。
- **01~03 支柱与工程层**：先有大纲（到原子概念标题），内容按主题逐篇填充。若要把一个主题拆得更细，先在 README 列出，不要擅自散开。
- 当前阶段：00 已完整，01 各支柱为大纲（细化到原子概念标题），02/03/09 为模块大纲。

## 编辑/新增笔记的工作流
1. 确定该笔记属于哪个支柱目录。
2. 严格按上述五段结构写。
3. 文件名用 kebab-case（如 `virtual-dom-reconciliation.md`）。
4. 更新本模块 `README.md` 的链接。
5. 检查所有交叉引用链接有效（双向打通）。
6. 提交格式：`docs(<模块>): <简述>`，例如 `docs(01-4): 新增虚拟DOM原子笔记`。

## 约定与禁忌
- **语言**：正文中文，技术术语保留英文（如 DOM、Reconciliation、SSR、CSP）。
- **代码**：伪代码，不写具体语言实现（v1 阶段）。如需具体语言示例，标注是 v2 内容。
- **不改大纲结构**：如发现大纲遗漏，记录并继续，最后统一与用户讨论，不擅自重构目录。
- **不引入新顶层目录**：5 大模块结构已定，新增内容放进对应模块。
- `.zcode/`、`.idea/`、`.git/` 已在 `.gitignore` 或不应入库，勿提交。

## v2 语言绑定层（规划中）
`90-lang/` 将是 **React + Next.js + TypeScript + Tailwind** 落地层。规划结构：
- `90-lang/nextjs/`：Next.js 基础设施（App Router 骨架/路由/渲染模式/数据获取/部署）
- `90-lang/react/`：React 核心（组件/Hooks/状态/性能/错误边界）
- `90-lang/styling/`：样式方案（Tailwind/设计令牌/主题/CSS 架构）
- `90-lang/state/`：状态层（Zustand/TanStack Query/表单状态）
- `90-lang/engineering/`：工程化（构建/测试/规范/CI-CD）
- `90-lang/examples/`：完整实战串联（示例项目 + `KNOWLEDGE-MAP.md` 代码↔v1/v2知识点映射）

v2 笔记结构：`> 对应 v1：[链接]` 开头，五段式（它解决什么/怎么实现/为什么这样写/常见坑/关联）。编辑 v2 笔记同样遵循 v1 的五段式结构和交叉引用规范。
